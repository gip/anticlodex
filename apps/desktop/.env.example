VITE_WORKOS_CLIENT_ID=client_your_client_id

# Optional overrides; these defaults are built into the app.
# VITE_WORKOS_REDIRECT_URI=anticlodex://auth/callback
# VITE_API_URL=http://localhost:3001

# Required only when running the local Claude agent.
# ACX_ENABLE_CLAUDE_AGENT=1
# ANTHROPIC_API_KEY=sk-ant-your_api_key
